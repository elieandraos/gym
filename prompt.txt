Currently my workout table has on category (enum in migration)
I would like it to have multiple categories, we can still use the enum as source of truth.
I do not wish to have an extra table with many to many relations (keep the table field as string if possible) as those categories are only informative, and just used in filters in the workout index page.
For ex: 'plank' can have 'core,abs' , 'Incline smith' can have 'Chest, Shoulders, Lats' etc...

Do not create a new migration if you want to change fields in workouts table, just update the existing one. i can truncate and do the work manually in production.
Update the current workout seeder to match the new implementation but the workouts/category should still exists.
