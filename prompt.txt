resources/js/Pages/Admin/Members/Show.vue
Comment on lines +74 to 76
const { activeBooking, scheduledBookings } = props
const { last_body_composition: lastBodyComposition } = props.member || {}
const isTraining = computed(() => !!activeBooking)
@coderabbitai
coderabbitai bot
8 minutes ago
⚠️ Potential issue | 🟠 Major

Reactivity issue: destructured activeBooking loses reactivity in computed.

The activeBooking is destructured from props at line 74, then used in the computed at line 76. Since destructuring happens outside the computed function, if activeBooking changes, isTraining won't update.

🔧 Proposed fix
-const { activeBooking, scheduledBookings } = props
-const { last_body_composition: lastBodyComposition } = props.member || {}
-const isTraining = computed(() => !!activeBooking)
+const { last_body_composition: lastBodyComposition } = props.member || {}
+const isTraining = computed(() => !!props.activeBooking)
Then update template references to use props.activeBooking and props.scheduledBookings, or keep the destructuring only for non-reactive uses like passing to child components.



resources/js/Pages/Admin/Members/Show.vue
    member: { type: Object, required: true },
    activeBooking: { type: Object, default: null },
    scheduledBookings: { type: Array, default: () => [] },
    completedBookings: { type: Array, default: () => [] },
@coderabbitai
coderabbitai bot
8 minutes ago
🧹 Nitpick | 🔵 Trivial

Unused prop: completedBookings is defined but not used.

The completedBookings prop is defined and passed from the controller but isn't referenced in the template or script. If it's intended for future use, consider adding a comment; otherwise, it can be removed from both the component and controller to reduce payload size.



resources/js/Pages/Admin/Bookings/Show.vue
    member: { type: Object, required: true },
    trainer: { type: Object, required: true },
    bookingSlots: { type: Array, required: true },
    bookingStats: { type: Object, required: true },
@coderabbitai
coderabbitai bot
9 minutes ago
🧹 Nitpick | 🔵 Trivial

Unused required prop: bookingStats is defined but not used.

The bookingStats prop is marked as required: true but isn't referenced in the component. If it's intended for the BookingSessions child component, pass it down; otherwise, consider removing it or making it optional.
