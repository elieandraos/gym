I want you to implement a trello like UI for booking slots circuits workouts.
It will live on the booking slot show page.
it will have a column (~30% of width) , the column will have a header (detached) on top with gray/zinc bg and circuit name inside it (circuit 1 by default)
The title is clickable which will edit inline textbox the circuit workout name.
under the title, the column body, also in gray/zinc with 'add workout' that will always at the bottom of the column
When clicking add Workout, a modal will appear (it will have like the old workoutform) a radio button (weight, duration), if weight is selected (default selection),
it will have 3 rows with 2 textboxes (reps and weight) those are the BookingSlotCircuitWorkoutSet. once saved, it will appear in a white rounded box inside the column body.

There is a Modal component inside Components/Layout, use that (also tell me where it is used across the app)
The column container is scrollable horizontally.
By default the page is blank, with one add circuit transaparent button covering on column width, once clicked the column will appear with default title and the add circuit button will shift to the right of the column.
